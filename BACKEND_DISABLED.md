# バックエンド配信停止

このリポジトリは画面配信用です。GASバックエンドをここから配信してはいけません。

- 正本: `katudouhoukoku_cloud_work/backend`
- 保存先の振り分け: 正本GASの `systemKey=8`
- `.clasp.json` と旧 `backend/Code.gs` は、誤上書きを防ぐため削除済みです。

画面変更後も、バックエンドの更新は正本リポジトリだけで行ってください。
